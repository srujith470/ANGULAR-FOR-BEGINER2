import { HighlitedDirective } from './highlited.directive';

describe('HighlitedDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlitedDirective();
    expect(directive).toBeTruthy();
  });
});
